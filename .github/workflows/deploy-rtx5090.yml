name: deploy-ubuntu-2404-rtx5090

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type DEPLOY to authorize the test-PC deployment'
        required: true
        default: ''
      model:
        description: 'Ollama model to install'
        required: true
        default: 'qwen3:30b'

permissions:
  contents: read

# Shared across every GPU-bound workflow in this repo (see
# docs/WORKSPACE_RUNNER_POOL.md): only one job that touches the RTX 5090s may run at a
# time, regardless of how many `gpu`-labeled runner instances exist, so benchmark/
# resource evidence is never measured under contention.
concurrency:
  group: gpu-rtx5090-exclusive
  cancel-in-progress: false

jobs:
  deploy:
    if: ${{ inputs.confirm == 'DEPLOY' }}
    runs-on: [self-hosted, gpu]
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - name: Verify preinstalled NVIDIA stack
        run: |
          nvidia-smi
          test "$(nvidia-smi --query-gpu=name --format=csv,noheader | grep -c 'RTX 5090')" -ge 2
      - name: Deploy application and local AI stack
        env:
          THREE_AGENT_MODEL: ${{ inputs.model }}
          THREE_AGENT_REPO_REF: main
          THREE_AGENT_MIN_DRIVER_MAJOR: '590'
          THREE_AGENT_REQUIRED_RTX5090_COUNT: '2'
          OLLAMA_CONTEXT_LENGTH: '65536'
        run: bash scripts/setup_ai_stack_ubuntu2404.sh
      - name: Final status
        run: |
          3agent smoke
          ollama ps
          nvidia-smi
