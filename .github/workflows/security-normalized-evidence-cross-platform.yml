name: security-normalized-evidence-cross-platform

on:
  push:
    branches: [main]
    paths:
      - 'src/three_agent/security_monitoring/normalized_evidence.py'
      - 'tests/test_security_normalized_evidence.py'
      - '.github/workflows/security-normalized-evidence-cross-platform.yml'
  pull_request:
    paths:
      - 'src/three_agent/security_monitoring/normalized_evidence.py'
      - 'tests/test_security_normalized_evidence.py'
      - '.github/workflows/security-normalized-evidence-cross-platform.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  normalized-evidence:
    name: normalized-evidence (${{ matrix.os }}, py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.11', '3.12']
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install project dependencies
        run: python -m pip install -e "."
      - name: Compile normalized evidence module and tests
        run: python -m compileall -q src/three_agent/security_monitoring/normalized_evidence.py tests/test_security_normalized_evidence.py
      - name: Run normalized evidence security tests
        run: python -m unittest tests.test_security_normalized_evidence -v
